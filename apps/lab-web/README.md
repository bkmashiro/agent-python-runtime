# Pysolate Lab Web — execution replay

A zero-dependency, fixture-backed, read-only debugger for canonical Lab v1
evidence. Its primary interaction is an operation-boundary replay:

```text
recorded run
  → sele...[truncated]