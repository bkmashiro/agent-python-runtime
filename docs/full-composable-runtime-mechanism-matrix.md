# Full Composable Runtime mechanism matrix

Status: **Deterministic implementation decision record.**
Date: 2026-08-13

| Mechanism | Decision | Exact bounded result | Primary evidence | Smallest next decision |
|---|---|---|---|---|
| Typed feature/mode resolution | Implemented Experimental | Zero-default `MechanismSet`; invalid dependencies fail before artifact parsing; stable selected/fallback/off evidence | `runtime/mechanisms_test.go` | Decide public names only after real acceptance |
| Staged observation identity | Implemented Experimental | Versioned one-shot Host record binds stream/workflow/source/suite/occurrence/args/spec/handler/plan/grant/freshness/expiry/privacy/parent; exact final-source bind and terminal ownership | `runtime/streaming/observation_test.go` | Keep Guest-local eager table private; do not create argument-global cache |
| Portable immutable workspace roots | Implemented Experimental | Expected-base seal, recursive lineage, Capsule rebind, explicit select, immutable enforcement and body-free materialization/garbage counts | `runtime/workspace/branch_root_test.go`, integrated E2E | General three-way merge only if real acceptance requires it |
| Structured recursive Subagents | Implemented Experimental | Versioned descriptor, bounded fan-out/depth, fresh Runner per child, private roots, explicit select, invalid/cancel cleanup, relative timeline | `runtime/subagent`, integrated real-Guest E2E | Evaluate scheduler ergonomics and useful overlap on the approved real workload |
| Host-instrumented Agent Functions | Implemented Experimental, narrowed | Binary admission, canonical full invocation identity, project-private bounded retention, corruption/eviction recompute, no fabricated effects in the instrumented fixture | `runtime/agentfunction/cache_test.go` | Jointly decide whether to build a Guest admission profile; do not call current Guard arbitrary-Python purity |
| Arbitrary Guest-Python purity | Deferred for joint review | Current Host `Guard` cannot prove absence of clock/random/dynamic import/undeclared filesystem/unknown imported behavior in arbitrary Guest source | unchecked Track 4 admission gate; documented non-claim | Design a complete Guest profile/instrumentation contract or keep arbitrary code `not_cacheable` |
| Independent single-flight | Implemented Experimental | Retention-independent leader/follower collapse, follower cancellation isolation, panic/error cleanup, identity partitioning | `runtime/agentfunction/singleflight_test.go` | Measure duplicate concurrency in real acceptance |
| Single-wait fresh re-evaluation | Implemented Experimental | Graph/root-bound explicit state, Guest close at wait, fresh Guest at resume, observation refresh, transitive invalidation, eviction recompute | `runtime/workflow/evaluator_test.go`, integrated real-Guest E2E | General multi-wait DAG scheduler only if the approved task needs it |
| General multi-wait workflow engine | Deferred for joint review | Not required by the north-star; would broaden into scheduler/compiler work | Track 6 decision ledger | Reopen only from a concrete acceptance blocker |
| Integrated composable north-star | Implemented Experimental | Two real children complete before parent EOF; select → cache/single-flight → wait/fresh resume; parent-invalid disposal; all-off fallback | `integration/e2e/composable_test.go` | Run the separately approved repository-shaped acceptance, not during this Megagoal |
| Never-served prepared runtime | Implemented Experimental | One initialized single-use module per selected Engine; no request reuse; unused slot destruction; second Run fresh fallback; `/tmp` freshness | prepared parity focused E2E and `PreparedState` | Measure startup/request/teardown phases on approved workloads |
| Linux memory COW | Implemented Experimental; full Guest qualification in progress | `cow-fixed` artifact profile plus sealed memfd baseline; every request gets a MAP_PRIVATE single-use mapping and every served/cancelled/failed slot is closed/unmapped, never reset into a pool; shape drift is rejected | [`evidence/linux-cow-low-level.json`](evidence/linux-cow-low-level.json); history `b405c58`, `f6ced98`; current Linux mapping tests | Build an exact-source `cow-fixed` CPython/WASI artifact and run the fresh/prepared/COW outcome-isolation matrix before performance interpretation |
| General filesystem three-way merge | Deferred for joint review | North-star only needs explicit select; merge adds conflict policy and publication semantics | Track 2 decision ledger | Select a concrete merge policy from a real task before implementation |
| Real external writes/effects | Out of scope, denied | Streaming projection and Broker pre-seal barrier remain the safety contract | existing capability/streaming tests | Separate external-effect intent/approval/journal/reconciliation Megagoal |

All deferred mechanisms preserve an explicit ordinary fresh/off fallback. None is silently simulated or reported as Current production behavior.
