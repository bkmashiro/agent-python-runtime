# Copy-paste Megagoal Prompt

Work continuously in `/Users/yuzhe/projects/agent-python-runtime` on branch `main` and execute:

`/Users/yuzhe/projects/agent-python-runtime/.hermes/plans/2026-08-12_025644-research-playback-branching-determinism-megagoal.md`

Read the entire roadmap and every source-of-truth document it names before editing. Inspect live Git state first; do not reset, discard, overwrite or repeat existing work. Treat roadmap checkboxes as source of truth and update them only after real verification.

This is a broad integrated megagoal, not a single branching feature. Complete all required tracks: playback-baseline productization; identity/SHA classification and justified simplification; a Lab-ready Host observation/correlation contract; honest WASI/filesystem observation; a measured content-addressed deduplicated research-store prototype for long/swarm-shaped traces; fresh-Guest counterfactual branches at capability-operation boundaries; a bounded deterministic-verification profile; the second typed `sources.benchmark_manifest()` research source; semantic inspect/compare/branch/store commands; and the combined real-Guest parent/playback/children/dedup demonstration.

Preserve the architecture boundary: Runtime owns execution, authority, capability operations, workspace state and bounded Host evidence; a future independent Pysolate Lab/Harness owns Agent turns, provider input/output, full trace semantics, branch-DAG UX, retention/redaction, queries and swarm analysis. Build Runtime contracts and a bounded local research prototype now, not the full GitLens-like GUI or hosted Lab.

Branching means start a fresh Guest from the parent's original request/initial workspace, strictly replay the prefix, then follow a fully sealed Host-owned override/recorded/live-read-only suffix. Never restore Python heap/WASM memory, expand authority mid-Run, or let Agent code choose fork policy. A branch may differ from its parent outcome; bind parent/fork/child lineage instead of forcing parent final identity.

Do not use SHA-256 decoratively. Classify every digest as content, authority, artifact/config, evidence/relation or redundant convenience. Prefer storing each immutable semantic body once and referencing it; remove only redundancy with no independent admission, streaming-validation, lookup/dedup, privacy-comparison or tamper-localization value. Digests remain Host/Lab bookkeeping and are not Agent-controlled or injected into Agent context by default.

Use TDD for every code slice: prove RED, implement minimally, prove GREEN with proportional race/real-Guest gates, update the roadmap, make a signed local commit, then continue. Do not push intermediate commits. Do not stop after one successful slice. Use the real artifact `/private/tmp/pysolate-current-artifact/dist-272811/agent-python-runtime.wasm`; loopback-only fixtures; no Docker, paid cloud, public network or credentials.

Never add shell, subprocess, ambient network, generic Agent HTTP, Guest URL/path/query, POST/write effects, credentials, browser/Computer, VM, pinned sessions, Effect Plane, transactions, package installation or plugin marketplace. Do not claim bytecode/local-variable omniscience, arbitrary Python determinism, full-Agent replay, or heap checkpoint restoration.

At the complete final gate, run all Go/vet/build/race, real Guest E2E, Python, ABI/tamper/privacy/docs/CLI/store benchmark gates and independent read-only review. Fix every blocker. Only then push `main` once and verify valid signatures, clean worktree and `HEAD == origin/main`. Stop only when all required checkboxes are complete or a roadmap-listed product/resource/risk boundary is proven.
